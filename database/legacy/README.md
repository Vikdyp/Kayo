# Legacy Database Exports

This directory is for documentation around old database exports and schema
snapshots.

Local dump files belong in `database/legacy/local/`, which is ignored by Git.
Do not commit production dumps or raw user data.
